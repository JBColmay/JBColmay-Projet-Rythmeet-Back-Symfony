{% extends 'base.html.twig' %}

{% block title %}Edit Concert{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center mt-5">Modifier un concert</h1>

    {{ include('concert_admin/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="container">
    <div class="row justify-content-center">
    <div class="col-md-6">
    <a href="{{ path('app_concert_admin_index') }}" class="btn btn-dark">Retour</a>
    </div>
    </div>
    </div>

    {{ include('concert_admin/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center mt-5">Créer un nouvel utilisateur</h1>

    {{ include('user_admin/_form.html.twig') }}
    <div class="container">
    <div class="row justify-content-center">
    <div class="col-md-6">
    <a href="{{ path('app_user_admin_index') }}" class="btn btn-dark">Retour</a>
    </div>
    </div>
    </div>
{% endblock %}
